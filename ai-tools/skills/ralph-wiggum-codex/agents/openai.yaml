interface:
  display_name: "Ralph Wiggum"
  short_description: "Run iterative Ralph loops in Codex."
  default_prompt: "Use $ralph-wiggum-codex to execute or manage a Ralph loop via the bundled scripts in the current sandbox only; do not escalate permissions or retry outside the sandbox."

policy:
  allow_implicit_invocation: false
